# Example Data

This directory contains tiny JSONL examples for smoke tests. The audio paths
are placeholders; use `its-perturb --dry-run` to preview perturbation manifests
without requiring the wav files.

For real experiments, replace `audio_manifest.demo.jsonl` with rows containing
real `audio_path`, `text`, and `seed_id` fields.

